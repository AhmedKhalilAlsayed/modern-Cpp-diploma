{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build main",
			"type": "shell",
			"command": "g++",
			"args": [
				"-g", // include debug symbols
				"${workspaceFolder}/main.cpp",
				"-o",
				"${workspaceFolder}/main"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}